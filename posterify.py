from enum import Enum
import numpy as np
from utils import load_image
import cv2


# def detect_edges(img):
#     np_img = np.array(img, dtype=np.float32)
#     return


# def group_colors(img, k=5):
#     np_img = np.array(img, dtype=np.float32)
#     return
